---
phase: 01-foundation-and-config-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/lib.rs
  - src-tauri/src/main.rs
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/App.tsx
  - src/main.tsx
  - index.html
  - src-tauri/tauri.conf.json
  - src-tauri/build.rs
  - src/components/SettingsPanel.tsx
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/config.rs
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "Developers can build the project on Windows 10/11 with documented prerequisites"
    - "Application launches and displays a Chinese-language settings interface"
    - "Tauri command system works for frontend-backend communication"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Tauri v2 dependencies and configuration"
      contains: 'tauri = { version = "2", features = ["tray-icon"] }'
    - path: "src/App.tsx"
      provides: "Main React component with Chinese UI"
      min_lines: 50
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri setup and command registration"
      exports: ["run"]
    - path: "src/components/SettingsPanel.tsx"
      provides: "Utilitarian Chinese settings UI"
      min_lines: 80
  key_links:
    - from: "src/App.tsx"
      to: "src/components/SettingsPanel.tsx"
      via: "React import and render"
      pattern: "import.*SettingsPanel"
    - from: "src/App.tsx"
      to: "src-tauri/src/commands/mod.rs"
      via: "Tauri invoke calls"
      pattern: "invoke\\("
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/mod.rs"
      via: "module inclusion"
      pattern: "mod commands"
---

<objective>
Create a buildable Tauri v2 application skeleton with React + TypeScript frontend, baseline Rust-backend command wiring, and a utilitarian Chinese-language settings interface.

Purpose: Establish the foundation for all subsequent phases — Tauri app shell, frontend-backend communication, and basic UI structure.

Output: Working Tauri dev environment, command/event infrastructure, and Chinese settings UI shell.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-and-config-spine/01-CONTEXT.md

# Key constraints from CONTEXT.md:
# - D-15: Phase 1 must ship a usable Chinese-language settings experience
# - D-16: Phase 1 UI only covers Phase 1 concerns (no premature exposure of later-phase surfaces)
# - D-17: Phase 1 UI should be plain and utilitarian rather than highly polished
# - D-18: Landing view treats configuration status and environment health as equally important
# - PLAT-01: App runs on Windows 10/11 only

# Key patterns from ARCHITECTURE.md:
# - Tauri commands for request-response (Rust → Frontend)
# - Tauri events for push notifications (Rust → Frontend)
# - React + TypeScript + Vite for frontend
# - dirs crate for platform-appropriate config paths
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Tauri v2 project with React + TypeScript</name>
  <files>src-tauri/Cargo.toml, src-tauri/tauri.conf.json, package.json, tsconfig.json, vite.config.ts, src/main.tsx, index.html</files>
  <action>
Create a new Tauri v2 project with React + TypeScript template. Configure:

1. Run: `cargo tauri init --name "EVE Local Alert" --window-title "EVE Local Alert"`
2. In Cargo.toml, add Tauri v2 dependencies:
   ```toml
   [dependencies]
   tauri = { version = "2", features = ["tray-icon"] }
   serde = { version = "1.0", features = ["derive"] }
   serde_json = "1.0"
   dirs = "5.0"
   ```
3. In tauri.conf.json, set Windows-specific config:
   - Enable tray icon feature
   - Set window title to "EVE Local Alert"
   - Configure for Windows platform only (no cross-platform promises per PLAT-01)
4. Create package.json with React 18+, TypeScript 5.x, Vite 5.x+
5. Configure Vite for HMR and Tauri integration
6. Create index.html with proper meta tags and Chinese charset
7. Create src/main.tsx as React entry point

Per D-15 and D-17: Keep UI plain and utilitarian — no elaborate styling, just functional Chinese text.
  </action>
  <verify>
    <automated>cargo tauri dev</automated>
  </verify>
  <done>Tauri app launches, window displays, dev server starts without errors</done>
</task>

<task type="auto">
  <name>Task 2: Set up baseline Tauri command system and event channels</name>
  <files>src-tauri/src/main.rs, src-tauri/src/lib.rs, src-tauri/src/commands/mod.rs</files>
  <action>
Create the Rust backend command structure:

1. Create src-tauri/src/commands/mod.rs with command module organization
2. Create placeholder commands for Phase 1 concerns:
   ```rust
   // src-tauri/src/commands/config.rs (placeholder for Plan 02)
   #[tauri::command]
   async fn save_config() -> Result<(), String> {
       Ok(())
   }

   #[tauri::command]
   async fn load_config() -> Result<String, String> {
       Ok(String::new())
   }
   ```
3. Register commands in lib.rs using tauri::Builder:
   ```rust
   fn run() {
       tauri::Builder::default()
           .invoke_handler(tauri::generate_handler![
               commands::config::save_config,
               commands::config::load_config,
           ])
           .run(tauri::generate_context!())
           .expect("error while running tauri application");
   }
   ```
4. Set up event channel infrastructure (for future Phase 2+ use):
   ```rust
   // Event emission example pattern
   app.emit("config-saved", payload)?;
   ```

Per ARCHITECTURE.md: This establishes the command pattern that all phases will follow.
  </action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Commands compile without errors, Tauri builder structure in place</done>
</task>

<task type="auto">
  <name>Task 3: Create basic Chinese settings UI (utilitarian style)</name>
  <files>src/App.tsx, src/components/SettingsPanel.tsx</files>
  <action>
Create a plain, utilitarian Chinese settings interface:

1. Create src/components/SettingsPanel.tsx with:
   - Section for "配置状态" (Config Status) showing current config file path and last modified time
   - Section for "环境检查" (Environment Check) showing OpenCV status (placeholder for Plan 03)
   - Save and Load buttons for configuration (connected to placeholder commands)
   - Plain styling: no elaborate design, just functional layout with Chinese labels

2. Update src/App.tsx to:
   - Render SettingsPanel as the main view
   - Set document title to Chinese
   - Use simple layout (no routing yet — Phase 1 scope only per D-16)

Per D-15: Must be usable Chinese-language settings experience
Per D-17: Plain and utilitarian, not polished
Per D-18: Configuration status and environment health are equally prominent
  </action>
  <verify>
    <automated>npm run tauri dev</automated>
  </verify>
  <done>App displays Chinese settings panel with config status and environment check sections, Save/Load buttons visible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Rust backend | User input crosses Tauri invoke boundary |
| frontend→filesystem | Config file read/write operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Config file paths (input validation) | accept | Config paths are system-generated via `dirs` crate, not user-input in Phase 1 |
| T-01-02 | Information Disclosure | Config file contents (sensitive data) | accept | Phase 1 config contains only non-sensitive settings |
| T-01-03 | Denial of Service | File I/O operations (malformed JSON) | mitigate | Use serde JSON with Result handling, show user-friendly error per D-04 |

No ASVS L1 requirements apply to this foundational phase. Threats are low-risk (no auth, no PII, no network).
</threat_model>

<verification>
- Tauri dev server starts without errors
- Window displays with Chinese title
- Settings panel shows config status and environment check sections
- Save/Load buttons are present (functionality in Plan 02)
- Commands compile successfully
- Platform constraint documented (Windows 10/11 only)
</verification>

<success_criteria>
- Developers can run `cargo tauri dev` and see a working app window
- Settings panel displays Chinese text correctly
- Save/Load buttons visible and connected to command infrastructure
- Project builds on Windows 10/11 with clear setup instructions
- No cross-platform promises (per PLAT-01)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-config-spine/01-01-SUMMARY.md`
</output>
